import React from 'react';

const JoinWorkspace = () => {
    return (
        <div>
            <h2>Join a Workspace</h2>
            {/* Form to join a workspace */}
        </div>
    );
};

export default JoinWorkspace;