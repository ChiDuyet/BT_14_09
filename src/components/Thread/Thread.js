import React from 'react';

const Thread = () => {
    return (
        <div>
            <h2>Thread Messages</h2>
            {/* Display thread messages */}
        </div>
    );
};

export default Thread;